import React from 'react'
import {Header} from '../../../../payload/payload-types'

const MobileNav = (
  { header } :
    {
      header: Header
    }
) => {
  return <div>mob</div>
}

export default MobileNav;
